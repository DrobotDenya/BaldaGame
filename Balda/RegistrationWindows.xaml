<Window x:Class="Balda.RegistrationWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RegistrationWindows" Height="394.521" Width="275.651" Closed="Window_Closed">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="23"/>
            <Setter Property="Text" Value=""/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Width" Value="140"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Foreground" Value="#FF1CF112"/>
            <Setter Property="Content" Value="Register"/>
        </Style>
    </Window.Resources>
    <Grid Background="#FFAEB52C">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Height="292" Width="150" Grid.RowSpan="4">
            <Label Content="Nickname"/>
            <TextBox x:Name="tbNickname" BorderThickness="3"/>
            <Label Content="First Name"/>
            <TextBox x:Name="tbFirstName" />
            <Label Content="Second Name"/>
            <TextBox x:Name="tbSecondName"/>
            <Label Content="Password"/>
            <TextBox x:Name="tbPassword"/>
            <Label Content="ConfirmPassword"/>
            <TextBox x:Name="tbConfPassword"/>
        </StackPanel>
        <Button x:Name="btnRegister" Click="BtnRegisterClick" Grid.Row="4">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-8.803"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>
    </Grid>
</Window>
